{
  "name": "modern-missing-version",
  "description": "A modern request whose _meta omits the required protocolVersion returns JSON-RPC -32602 Invalid params. Target from design section Modern request envelope. Currently unimplemented: handlers ignore _meta and serve the request, so this mismatches.",
  "steps": [
    {
      "request": {
        "jsonrpc": "2.0",
        "id": 103,
        "method": "tools/list",
        "params": {
          "_meta": {
            "io.modelcontextprotocol/clientCapabilities": {},
            "io.modelcontextprotocol/clientInfo": {"name": "probe", "version": "0"}
          }
        }
      },
      "expect": {
        "jsonrpc": "2.0",
        "id": 103,
        "error": {"code": -32602, "message": "Invalid params"}
      }
    }
  ]
}
